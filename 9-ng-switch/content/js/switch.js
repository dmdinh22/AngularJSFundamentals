var thing = <dynamicValue>;
    switch (thing) {
        case 'A':
            // do something when "thing" is A
        case 'B':
            // do something when "thing" is B
        default:
            // do something default
    }